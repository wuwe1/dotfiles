- clean: ['~']

- link:
    ~/.bash_profile: 
    ~/.bashrc:
    ~/.gitconfig: 
    ~/.gitignore_global:
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc: 
    ~/.zsh:
    ~/.zshrc: 
    ~/.shell:
    ~/.pythonrc:
    ~/.inputrc:
    


