- clean: ['~']

- link:
    ~/.bash_profile: 
    ~/.gitconfig: 
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc: 
    ~/.zsh:
    ~/.zshrc: 
    ~/.shell:
    ~/.pythonrc:
    


